import 'package:flutter/material.dart';

class VGapWidget extends StatelessWidget {
  const VGapWidget({
    Key? key,
  }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const SizedBox(
      height: 20,
    );
  }
}
